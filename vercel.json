{
  "version": 2,
  "builds": [
    {
      "src": "littlelemon/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.12",
        "maxLambdaSize": "50mb",
        "buildCommand": "echo '--- START BUILD ---' && pip install -r requirements.txt && python manage.py collectstatic --no-input --clear && echo '--- COLLECTSTATIC DONE ---'"
      }
    }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/staticfiles/$1" },
    { "src": "/(.*)", "dest": "littlelemon/wsgi.py" }
  ]
}
